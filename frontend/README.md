# TaskFlow Frontend

This is the Next.js frontend for the TaskFlow application.

## 🚀 How to Run

1.  Make sure the **Backend** is running first (see root README).
2.  Navigate to this folder:
    ```bash
    cd frontend
    ```
3.  Install dependencies (if new):
    ```bash
    npm install
    ```
4.  Start the development server:
    ```bash
    npm run dev
    ```
5.  Open [http://localhost:3000](http://localhost:3000).

## 🛠️ Tech Stack

*   **Next.js 14** (App Router)
*   **Tailwind CSS** (Styling)
*   **Framer Motion** (Animations)
*   **Axios** (API Requests)

## 📁 Key Directories

*   `src/app`: Pages and Layouts
*   `src/components`: Reusable UI elements
*   `src/context`: State management (Auth, Theme)
*   `src/services`: API configuration
